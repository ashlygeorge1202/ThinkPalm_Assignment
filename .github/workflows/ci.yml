name: pytest + Allure

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Set up Java (needed by Allure)
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'

      # Step 3: Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # Step 4: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Step 5: Debug - list files to make sure tests exist
      - name: List project files
        run: ls -R

      # Step 6: Run pytest with Allure
      - name: Run pytest with Allure results
        run: |
          mkdir -p reports/allure-results
          pytest --maxfail=1 --disable-warnings -v --alluredir=reports/allure-results

      # Step 7: Upload raw Allure results
      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: reports/allure-results

      # Step 8: Install Allure CLI
      - name: Install Allure CLI
        if: always()
        run: |
          npm install -g allure-commandline --no-update-notifier --no-fund --silent

      # Step 9: Generate HTML report
      - name: Generate Allure HTML report
        if: always()
        run: |
          allure generate reports/allure-results -o reports/allure-report --clean

      # Step 10: Upload HTML report
      - name: Upload Allure HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: reports/allure-report
